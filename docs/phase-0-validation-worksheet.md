# Phase 0 验证工作单(go/no-go)

> **检查 1 + 检查 2 已由 Claude 浏览器只读勘查完成(2026-06-04,全程零提交)。检查 3 仍需你补。**
>
> **检查 1(提交机制)✅**:后台 = layui + jQuery「51漫画管理系统」。新增表单是列表页内联 layui layer(非 iframe),`lay-event="add"` 打开。提交 = `<button lay-submit lay-filter="save">` → layui `form.on('submit(save))` → `$.post`/`$.ajax` 到 **`/admin/webarticle/save`**(**XHR/AJAX,非原生 form 提交**)。表单**无 token 命名字段**(CSRF 大概率靠 session cookie)。→ 计划已据此定:U2 走 XHR、复用页面自身 layui 提交;U3 加 fetch/XHR spy 通道。
>
> **检查 2(草稿态)✅**:有!`status` 字段 = **隐藏 / 显示**。自动发成"隐藏"、审核后转"显示"即"先草稿后转正"(R9 可行)。
>
> **附带(字段映射校验)✅**:正文 = Quill `#editor`(现有方案成立);字段 = media_id/title/subtitle/type(漫畫/動漫 二选一)/file 封面/status/description/published_at/html_content(正文 hidden)/tags[](checkbox 多选)。与现有 `DEFAULT_FIELD_MAPPING` 吻合,低漂移。
>
> **检查 3(瓶颈)✅ 已由浏览器实做发帖测得**:程序化填充 8 字段+Quill = **14ms**;保存 `POST /webarticle/save` 往返 **~0.4s**(无 CSRF 也"操作成功")。→ **单帖机械发布 < 0.5s**,所以"点发布"机械成本可忽略,**真瓶颈在内容生成+人审,不在点击**。实做了 3 条隐藏测试帖(id 110/111/112,可删)。
> **关键坑**:naive 点提交按钮会掉回原生 GET 不保存 → U2 须 POST /save 或正确触发 layui 提交。
>
> **检查 3(草稿合格率)⚠️ 仍需你用扩展在本站题材上实测**:本站是 NSFW 里番/成人动画介绍站,固定"51娘"口吻 + 逐里番作品事实 + 漢化/無修连结。AI 草稿合格率取决于能否命中这套小众风格且不编造作品事实/连结(幻觉风险高)。请用扩展在 5–10 个**本站真实题材**上生成、贴给我评。
>
> **🧹 清理**:我建了 3 条隐藏测试帖 **id 110/111/112**(标题带 TEST*勿用、status=隐藏、读者看不到)。按规矩我不替你硬删数据——请在后台列表搜 "TEST*勿用" 把这 3 条删掉。

---

> 目的:在写任何自动发布代码前,先验证三件事。三项都过 → 进 Phase 1;任一不过 → 回退"只自动化生成+填充、人工点发布"。
> 做法:在 Chrome 打开 admin 后台 `https://dx-999-adm.ympxbys.xyz/` 发帖页,按下面三块做,把【填这里】补上。

---

## 检查 1:真后台到底怎么"提交/发布"?(决定代码怎么写)

**步骤:**

1. 发帖页按 `F12` 打开开发者工具 → 切到 **Network(网络)** 标签 → 勾上 **Preserve log(保留日志)**。
2. 随便填一条测试帖(自家站,待会删),点【发布】。
3. 看 Network 里点发布瞬间冒出的请求:
   - 有没有一条 **POST** 请求?它的 **URL** 是什么?
   - 点开它的 **Payload/请求体**,里面有没有像 `csrf` / `token` / `_token` / `authenticity` 的字段?
4. 再切到 **Elements(元素)**,右键【发布】按钮 →【检查】:它是 `<button type="submit">`(在 `<form>` 里)还是一个 `<div>`/`<a>`/带 onclick 的按钮?

**填这里:**

- 提交方式(三选一):☐ form 原生 submit ☐ 具名按钮 click ☐ XHR/fetch 请求
- 若是 XHR:请求 URL = 【填这里】;请求体里有 CSRF/token 吗?☐ 有(字段名:\_\_\_\_) ☐ 无
- 发布按钮的选择器(检查面板里能看到,如 `button.publish-btn` / `#submit`)= 【填这里】

---

## 检查 2:有没有"草稿/不公开"态?(决定全自动档能不能做)

**步骤:** 看发帖表单里有没有"存为草稿 / 草稿 / 不公开 / 状态=草稿/待审"这类选项;发布后在列表里能不能把一条改回草稿;有没有"批量"操作。

**填这里:**

- 有"草稿/不公开"态吗?☐ 有 ☐ 无
- 能"批量把草稿转正式"吗?☐ 能 ☐ 不能 ☐ 不确定
- (无草稿态也不影响 v1——v1 审核在提交前把关;此项只决定以后全自动档)

---

## 检查 3:瓶颈基线 + 草稿合格率(决定自动发值不值)

**步骤 A(瓶颈):** 用正常流程人工发 ~10 条,粗略记三段时间:读/改草稿、填表、点发布跳转。看**"点发布"这一下**在总时间里占多少。

**填这里:**

- 发 10 条总耗时 ≈ 【填这里】分钟;其中"点发布+等跳转"占比 ≈ 【填这里】%
- 直觉:真正费时间的是 ☐ 写/改内容 ☐ 填表 ☐ 点发布

**步骤 B(草稿质量):** 让插件/AI 生成 ~10 条草稿,你标:可直接发 / 需小改 / 不合格。

**填这里:**

- 可直接发 **_ 条 / 需小改 _** 条 / 不合格 \_\_\_ 条

---

## go / no-go 判定(填完上面再看)

- ✅ **go(进 Phase 1)**:点发布确实占大头(说明自动提交有价值)**且** 草稿合格率够高(多数可直接发或小改)**且** 提交机制已搞清。
- ⛔ **no-go → 回退**:
  - 若"点发布"只占很小比例 → 自动提交收益小,**先只自动化"生成+填充",发布仍人工点**(零新不可逆风险)。
  - 若草稿合格率低 → **先改 prompt/生成质量**,别急着自动把次品推上域名(伤 SEO)。

**我的结论(贴回给我,我来判 + 改计划):** 【填这里】
